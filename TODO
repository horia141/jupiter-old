Commands to add:
* Add notion of goal order and commands to shift goals around.
* Add notion of metric order and commands to shift metrics around
* Add notion of task order and commands to shift tasks around
* Add command add subtask to a task
* Command to change title of a subtask
* Add nesting of subtasks
* Move subtask to be a child of a another subtask of of top-level task
* Add notion of subtask order and commands to shift subtasks around
* Archive subtasks
* Make move avoid circular paths!